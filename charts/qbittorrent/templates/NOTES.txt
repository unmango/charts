Open the qBittorrent WebUI:
{{- if .Values.httpRoute.enabled }}
{{- range .Values.httpRoute.hostnames }}

  http://{{ . }}
{{- end }}
{{- else }}

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ .Release.Name }} {{ .Values.service.port }}:http

and browse to http://localhost:{{ .Values.service.port }}
{{- end }}
{{- if not (include "secretName" .) }}

No auth.password is set, so qBittorrent printed a temporary WebUI password:

  kubectl logs --namespace {{ .Release.Namespace }} {{ .Release.Name }}-0 -c qbittorrent | grep -i password
{{- end }}
{{- if .Values.gluetun.enabled }}

All traffic leaves through the gluetun sidecar; the pod reports Ready only once
the tunnel is up.
{{- end }}
