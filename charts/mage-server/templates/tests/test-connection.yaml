apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mage-server.fullname" . }}-test-connection"
  labels:
    {{- include "mage-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mage-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
