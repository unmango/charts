apiVersion: v1
kind: Service
metadata:
  name: {{ include "mage-server.fullname" . }}
  labels:
    {{- include "mage-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mage-server.selectorLabels" . | nindent 4 }}
